import React, { Component } from "react";
import { Button, Text, View } from "react-native";
import { FlatList } from "react-native-gesture-handler";
import { MinifigsScreenProps as MinifigsScreenProps } from "../navigation/app-stacks";
import CocktailItem from "../components/SetsItem";

export default class DetailsScreen extends Component<DetailsScreenProps, {}> {
  render() {
    //const navigation: StackNavigationProp<RootStackParamList, "Cocktail">
    //const{navigation} = this.props;
    return (
    
    );
  }
}
